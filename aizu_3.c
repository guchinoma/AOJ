#include <stdio.h>
#include <stlib.h>
#include <string.h>

int main(){
    char numbers;
    int array[];
    while (scanf("%d", &numbers) == EOF){
        number_1, number_2 = strtok(numbers, " ");
        
    }
}